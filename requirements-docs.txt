# Docs build dependencies (for GitHub Pages deploy).
# Pinned to the versions known to work; bump together with mkdocs.yml.

mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
mkdocs-mermaid2-plugin>=1.0,<2
pymdown-extensions>=10.7,<11
