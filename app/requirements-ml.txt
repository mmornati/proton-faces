# proton-faces app — ML inference deps (optional)
# insightface >=2.0 supports Python 3.10+ and numpy>=1.26 (unlike 0.7.3,
# which had no cp312 wheels and relied on numpy aliases removed in 1.24+).
# The app image (python:3.12-slim) installs these. Unit tests use only
# requirements-core.txt. All of these are imported lazily at runtime
# (faces._load, clip._load).

pillow-heif>=1.0,<1.8
onnxruntime>=1.30.0,<1.31
insightface>=2.0,<3
tokenizers>=0.19,<0.24