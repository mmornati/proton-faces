# proton-faces dev / test deps (repo root)
# Core runtime deps only — ML deps (insightface/onnxruntime) are lazy-loaded
# and not needed to run the unit test suite.

-r app/requirements-core.txt
pytest>=9.0,<10.0
pytest-cov>=5.0,<6.0
ruff>=0.5,<1.0
pip-audit>=2.7,<3.0